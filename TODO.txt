* Integrate redis to store OAUTH2 access token, refresh token and auth code
* Build container (test and prod)
* tweak redis conf file as needed (remove data persistence if there, not sure what's default)
* Possible authentication/security hardening for redis
* break out node server into separate repo or consider monorepo
* add logging middleware for nodejs server (morgan)
* add error handling middleware for nodejs server
* Possibly convert spotify api to class syntax
* structure redis data better - look into sets and lists
* configure redis to use a unix socket
* need to generate authorized code - right now just storing original one
* react drag and drop (dnd) to drag and drop songs or something else
* Lock down CORS server-side
